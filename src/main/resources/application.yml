spring:
  profiles:
    active: dev   #多环境配置选择
  datasource:
    driver-class-name: com.mysql.jdbc.Driver
    url: jdbc:mysql://127.0.0.1/will
    username: root
    password: 123456
  jpa:
    hibernate:
      ddl-auto: create #运行的时候会自动创建表，即使已经存在该表
    show-sql: true #控制台看见sql语句

